[project]
name = "qwen-ranker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "accelerate",
    "aiohttp",
    "fastapi",
    "torch",
    "transformers",
    "uvicorn",
    "vllm>=0.6.0",
]
